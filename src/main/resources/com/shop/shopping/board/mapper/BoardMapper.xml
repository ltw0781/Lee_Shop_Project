<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace="매퍼 인터페이스 경로" -->
<mapper namespace="com.shop.shopping.board.mapper.BoardMapper">

<select id="list" resultType="Board">
        SELECT *
        FROM board
        ORDER BY no DESC
        LIMIT #{page.index}, #{page.rows}
    </select>

    <select id="read" resultType="Board">
        SELECT *
        FROM board
        WHERE no = #{no}
    </select>

    <insert id="insert">
        INSERT INTO board( board_type, category, title, writer, content, created_at, updated_at )
        VALUES ( #{boardType}, #{category},  #{title}, #{writer}, #{content}, now(), now())
    </insert>

    <!-- 게시글 수정 -->
    <update id="update">
        UPDATE board
            SET category = #{category}
                ,title = #{title}
                <!-- ,user_id = #{userId} -->
                ,writer = #{writer}
                ,content = #{content}
                ,updated_at = now()
        WHERE no = #{no}
    </update>

    <delete id="delete">
        DELETE FROM board
        WHERE no = #{no}
    </delete>

    <!-- 데이터 개수 조회 - [검색] -->
    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM board
        ORDER BY created_at DESC
    </select>

    <!-- 게시글 번호 최댓값 -->
    <select id="maxPk" resultType="int">
        SELECT MAX(no)
        FROM board
    </select>


</mapper>